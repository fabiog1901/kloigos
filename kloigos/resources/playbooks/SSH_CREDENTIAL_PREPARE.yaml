---
- name: Prepare SSH credentials placeholder
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: No-op SSH credential preparation
      debug:
        msg: "Kloigos SSH credential preparation hook is not configured."
