---
- name: Cleanup SSH credentials placeholder
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: No-op SSH credential cleanup
      debug:
        msg: "Kloigos SSH credential cleanup hook is not configured."
